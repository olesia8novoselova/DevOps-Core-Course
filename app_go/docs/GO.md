# Language Justification (Go Bonus)

I chose **Go** because it is a compiled language that produces a **single executable binary**, which is easy to ship and run in containers.  
Go also compiles fast and has a simple standard library (`net/http`) that is enough to build this API with minimal code.  
In addition, I already have experience in working with Go, so it's an opportunity for me to practice my skills.
