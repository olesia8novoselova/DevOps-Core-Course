apiVersion: v1
kind: Service
metadata:
  name: devops-info-python-service
  labels:
    app: devops-info-python
spec:
  type: NodePort
  selector:
    app: devops-info-python
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
